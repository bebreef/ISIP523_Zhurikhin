<Page x:Class="Pr15.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Pr15.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CatalogPage">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBox x:Name="tbSearch" Width="300" TextChanged="tbSearch_TextChanged" Margin="0,0,12,0"/>
            <ComboBox x:Name="cmbType" Width="220" SelectionChanged="cmbType_SelectionChanged" DisplayMemberPath="name"/>
        </StackPanel>
        <ListBox x:Name="lbParts" Grid.Row="1" MouseDoubleClick="lbParts_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="4">
                        <Image Source="{Binding image}" Width="80" Height="80" Stretch="Uniform" Margin="0,0,12,0"/>
                        <TextBlock Text="{Binding name}" FontWeight="Bold"/>
                        <TextBlock Text="{Binding ManufacturerName}"/>
                        <TextBlock Text="{Binding ShortDesc}"/>
                        <TextBlock Text="{Binding price, StringFormat='{}{0:N0} ₽'}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>